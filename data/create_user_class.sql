CREATE TABLE IF NOT EXISTS user_class (
	ucid INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid INTEGER NOT NULL,
	cid INTEGER NOT NULL,
	FOREIGN KEY (uid) REFERENCES user(uid),
	FOREIGN KEY (cid) REFERENCES class(cid)
	);

