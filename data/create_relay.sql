CREATE TABLE IF NOT EXISTS relay (
	rid INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY,
	zid INTEGER NOT NULL FOREIGN KEY REFERENCES zone(zid),
	number INTEGER NOT NULL,
	description VARCHAR(255) NULL
	);
