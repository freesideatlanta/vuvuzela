CREATE TABLE IF NOT EXISTS user_token (
	utid INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid INTEGER NOT NULL FOREIGN KEY REFERENCES user(uid),
	tid INTEGER NOT NULL FOREIGN KEY REFERENCES token(tid)
	);
