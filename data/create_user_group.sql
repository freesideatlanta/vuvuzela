CREATE TABLE IF NOT EXISTS user_group (
	ugid INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid INTEGER NOT NULL FOREIGN_KEY REFERENCES user(uid),
	gid INTEGER NOT NULL FOREIGN_KEY REFERENCES group(gid),
	);
